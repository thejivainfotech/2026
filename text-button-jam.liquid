https://jams-by-josh-cle.myshopify.com/
Down theme version 15.4.1
===============================================

{%- style -%}
  .store-locator-{{ section.id }} {
    padding: {{ section.settings.padding_top }}px 0 {{ section.settings.padding_bottom }}px 0;
    text-align: center;
    width: 100%;
    position: relative;
    overflow: hidden;
    
    {% if section.settings.bg_image != blank %}
      background-image: url('{{ section.settings.bg_image | image_url: width: 2000 }}');
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
    {% else %}
      background-color: {{ section.settings.bg_color }};
      background-size: 20px 20px;
    {% endif %}
  }
  .store-locator-{{ section.id }}::before {
    content: "";
    position: absolute;
    top: 0; left: 0; width: 100%; height: 100%;
    background: rgba(0,0,0, {{ section.settings.overlay_opacity | divided_by: 100.0 }});
    z-index: 1;
  }

  .store-locator-container {
    max-width: 1200px;
    margin: 0 auto;
    padding: 0 20px;
    position: relative;
    z-index: 2;
  }

.store-locator-title {
    font-weight: 400;
    font-size: 48px;
    text-transform: uppercase;
    color: {{ section.settings.text_color }};
    margin-bottom: 25px;
    line-height: 50px;
}
  .store-locator-btn {
    display: inline-block;
    background-color: {{ section.settings.btn_bg_color }};
    color: {{ section.settings.btn_text_color }};
    padding: 0px 20px;
    border-radius: 70px;
    text-decoration: none;
    font-weight: 400;
    font-size: 48px;
    text-transform: uppercase;
    transition: all 0.3s ease;
    border: 2px solid transparent;
  }

  @media screen and (max-width: 768px) {
    .store-locator-title { font-size: 48px; }
    .store-locator-btn { font-size: 28px; padding: 0px 50px;}
  }
{%- endstyle -%}

<section class="store-locator-{{ section.id }}">
  <div class="store-locator-container">
    {% if section.settings.heading != blank %}
      <h2 class="store-locator-title">{{ section.settings.heading }}</h2>
    {% endif %}
    
    {% if section.settings.button_label != blank %}
      <a href="{{ section.settings.id_link }}" class="store-locator-btn cus_font_anton">
        <span>{{ section.settings.button_label }}</span>
      </a>
    {% endif %}
  </div>
</section>

{% schema %}
{
  "name": "Store Locator Banner",
  "settings": [
    {
      "type": "text",
      "id": "heading",
      "label": "Heading",
      "default": "FIND US IN STORES"
    },
    {
      "type": "text",
      "id": "button_label",
      "label": "Button Label",
      "default": "ALL STORE LOCATIONS"
    },
    {
      "type": "url",
      "id": "id_link",
      "label": "Button Link"
    },
    {
      "type": "header",
      "content": "Background Settings"
    },
    {
      "type": "image_picker",
      "id": "bg_image",
      "label": "Background Image",
      "info": "Best size: 1920x600px"
    },
    {
      "type": "color",
      "id": "bg_color",
      "label": "Background Color (Fallback)",
      "default": "#ffffff"
    },
    {
      "type": "range",
      "id": "overlay_opacity",
      "min": 0,
      "max": 90,
      "step": 10,
      "unit": "%",
      "label": "Image Overlay Opacity",
      "default": 0
    },
    {
      "type": "header",
      "content": "Colors & Spacing"
    },
    {
      "type": "color",
      "id": "text_color",
      "label": "Heading Color",
      "default": "#2b1a46"
    },
    {
      "type": "color",
      "id": "btn_bg_color",
      "label": "Button Background",
      "default": "#2b1a46"
    },
    {
      "type": "color",
      "id": "btn_text_color",
      "label": "Button Text Color",
      "default": "#ffffff"
    },
    {
      "type": "range",
      "id": "padding_top",
      "min": 20,
      "max": 150,
      "step": 5,
      "unit": "px",
      "label": "Padding Top",
      "default": 80
    },
    {
      "type": "range",
      "id": "padding_bottom",
      "min": 20,
      "max": 150,
      "step": 5,
      "unit": "px",
      "label": "Padding Bottom",
      "default": 80
    }
  ],
  "presets": [
    {
      "name": "Store Locator Banner"
    }
  ]
}
{% endschema %}
